global.$viewpoint = null; // when the page is ready, a reference to the main <VIEWPOINT> @todo is this actually needed?


//// 
jQuery(function($) { // jQuery document ready


    //// Xx
    global.$viewpoint = $('#vp0'); // The main <VIEWPOINT> at startup, which will be updated by `viewpoint.js` as the player moves around the tarrain

});